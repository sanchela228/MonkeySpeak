<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NewAppMaui.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a submarine number ten" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Label
                x:Name="ConnectionStatusLabel"
                Text="Connecting..."
                HorizontalOptions="Fill" />

            <Label
                x:Name="AuthStatusLabel"
                Text="Not authenticated"
                HorizontalOptions="Fill" />

            <Label
                x:Name="ConnectionErrorLabel"
                TextColor="Red"
                IsVisible="False"
                HorizontalOptions="Fill" />

            <Button
                x:Name="CreateConnectionBtn"
                Text="Create connection"
                IsVisible="False"
                Clicked="OnCreateConnectionClicked"
                HorizontalOptions="Fill" />

            <Button
                x:Name="ConnectBtn"
                Text="Connect"
                IsVisible="False"
                Clicked="OnConnectClicked"
                HorizontalOptions="Fill" />

            <Button
                x:Name="ChangeConnectionBtn"
                Text="Change connection"
                Clicked="OnChangeConnectionClicked"
                HorizontalOptions="Fill" />

            <Button
                x:Name="FriendsBtn"
                Text="Friends"
                Clicked="OnFriendsClicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
